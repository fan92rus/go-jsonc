version: "2"
run:
  timeout: 2m
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - misspell
    - unconvert
    - bodyclose
    - noctx
    - errname
    - errorlint
    - nakedret
    - prealloc
    - gocyclo
    - gocognit
    - funlen
    - maintidx
    - lll
    - dupl
    - goconst
    - thelper
  settings:
    staticcheck:
      checks:
        - all
        - -SA1029
    gocyclo:
      min-complexity: 20
    funlen:
      lines: 60
      statements: 40
    gocognit:
      min-complexity: 30
    dupl:
      threshold: 100
formatters:
  enable:
    - gofmt
    - goimports
issues:
  max-issues-per-linter: 100
  max-same-issues: 5
output:
  formats:
    colored-line-number:
      path: stdout
